<UserControl x:Class="GflChibiDesktop2.DownloadSourcesDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
             Width="500" Height="250" Background="{DynamicResource SecondaryRegionBrush}" Foreground="{DynamicResource PrimaryTextBrush}" >
    <hc:SimplePanel>
        <Border Background="{DynamicResource PrimaryBrush}" Height="30" VerticalAlignment="Top">
            <hc:SimplePanel>
                <hc:SimpleText FontSize="{StaticResource SubHeadFontSize}" VerticalAlignment="Center" TextWrapping="Wrap" HorizontalAlignment="Stretch" TextAlignment="Center" Text="下载源设置" Foreground="{StaticResource PrimaryTextBrush}" Margin="30,0,30,0"/>
                <Image RenderTransformOrigin="0,0" UseLayoutRounding="True" RenderOptions.BitmapScalingMode="HighQuality" Height="24" Width="24" HorizontalAlignment="Left" Source="/hk416_angry.ico" VerticalAlignment="Top" Margin="3"/>
                <Button Width="24" Height="24" Style="{StaticResource ButtonIcon}" hc:IconElement.Geometry="{StaticResource ErrorGeometry}" Padding="0" HorizontalAlignment="Right" VerticalAlignment="Top" Command="hc:ControlCommands.Close" Margin="3" />
            </hc:SimplePanel>
        </Border>
        <Grid Margin="0,30,0,0">
            <Label Content="下载源：" HorizontalAlignment="Left" Height="28" VerticalAlignment="Top" Width="48" Margin="10,3,0,0" Padding="0" Background="{x:Null}" BorderThickness="0"/>
            <hc:TextBox x:Name="tb_downloadSource" VerticalAlignment="Top" Margin="58,3,10,0"  VerticalScrollBarVisibility="Disabled" MaxLines="1" IsReadOnly="True" Background="{x:Null}" BorderThickness="0" Padding="0"/>
            <ListBox x:Name="lb_sources" HorizontalAlignment="Left" Width="175" Margin="10,35,0,50" SelectionChanged="lb_sources_SelectionChanged" />
            <Label x:Name="lbl_source" FontSize="{StaticResource SubHeadFontSize}" Content="下载源" Background="{x:Null}" BorderThickness="0" VerticalAlignment="Top" Margin="195,35,10,0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="0"/>
            <hc:TextBox x:Name="tb_sourceInfo" Margin="195,65,10,50" Padding="0" Background="{x:Null}" IsReadOnly="True" Text="未选择下载源" VerticalContentAlignment="Top" VerticalScrollBarVisibility="Auto" BorderThickness="0" TextWrapping="WrapWithOverflow"/>
            <Button x:Name="btn_UpdateSources" Content="刷新下载源" HorizontalAlignment="Left" Height="30" VerticalAlignment="Bottom" Width="175" Margin="10,0,0,10" Click="btn_UpdateSources_Click"/>
            <Button x:Name="btn_OK" Content="确定" HorizontalAlignment="Right" Height="30" VerticalAlignment="Bottom" Width="100" Margin="0,0,120,10" Click="btn_OK_Click" />
            <Button x:Name="btn_Cancel" Content="取消" HorizontalAlignment="Right" Height="30" VerticalAlignment="Bottom" Width="100" Margin="0,0,10,10" Command="hc:ControlCommands.Close" />
        </Grid>
    </hc:SimplePanel>
</UserControl>
