<UserControl xmlns:hc="https://handyorg.github.io/handycontrol" x:Class="GflChibiDesktop2.NumericControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GflChibiDesktop2"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400" Height="50">
    <Grid Height="50" VerticalAlignment="Top" d:DataContext="{d:DesignInstance {x:Type local:NumericControl}, IsDesignTimeCreatable=True}" Margin="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="13*"/>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="{DynamicResource SecondaryBorderBrush}" BorderThickness="0, 1, 0, 0" Grid.ColumnSpan="2"/>
        <Label x:Name="PromptTextBox" Content="{Binding Path=PromptText}" Margin="10,3,4,0" VerticalAlignment="Top" Background="{x:Null}" BorderThickness="0" Padding="0,0,0,0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Right"/>
        <hc:NumericUpDown Margin="0,3,10,0" Value="{Binding Path=Value, UpdateSourceTrigger=PropertyChanged}" Minimum="{Binding Path=MinValue, Mode=OneWay}" Maximum="{Binding Path=MaxValue, Mode=OneWay}" VerticalAlignment="Top" Grid.Column="1"/>
        <Label x:Name="HintTextBox" Content="{Binding Path=HintText}" VerticalAlignment="Bottom"  Foreground="{StaticResource SecondaryTextBrush}" Grid.ColumnSpan="2" Background="{x:Null}" BorderThickness="0" Padding="0" VerticalContentAlignment="Bottom" HorizontalContentAlignment="Center" HorizontalAlignment="Stretch" Margin="10,0,10,4"/>
    </Grid>
</UserControl>
